{
  "name": "@jfa.dev/router",
  "version": "0.1.0-rc",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp build",
    "cf-typegen": "wrangler types",
    "check": "vp check --fix",
    "deploy": "vp check && vp lint --type-check && vp build && wrangler deploy",
    "dev": "wrangler dev",
    "format": "vp fmt",
    "lint": "vp lint",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "catalog:backend"
  },
  "devDependencies": {
    "@types/bun": "catalog:types",
    "@types/node": "catalog:types",
    "typescript": "catalog:build",
    "vite-plus": "catalog:build",
    "wrangler": "catalog:build"
  }
}
