{
  "name": "@jfa.dev/redirects",
  "version": "0.7.1",
  "private": true,
  "scripts": {
    "cf-typegen": "wrangler types",
    "check": "vp check",
    "deploy": "vp check && wrangler deploy",
    "dev": "wrangler dev",
    "format": "vp fmt",
    "lint": "vp lint",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "hono": "catalog:backend"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:cloudflare",
    "@types/bun": "catalog:types",
    "@types/node": "catalog:types",
    "@typescript/native-preview": "catalog:build",
    "typescript": "catalog:build",
    "vite-plus": "catalog:"
  }
}
