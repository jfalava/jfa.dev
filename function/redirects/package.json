{
  "name": "@jfa.dev/redirects",
  "private": true,
  "scripts": {
    "check": "vp check --fix",
    "dev": "bun --watch src/dev.ts",
    "format": "vp fmt",
    "lint": "vp lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "catalog:backend"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:types",
    "@types/bun": "catalog:types",
    "@types/node": "catalog:types",
    "typescript": "catalog:build",
    "vite-plus": "catalog:build"
  }
}
