{
  "name": "@jfa.dev/redirects",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "cf-typegen": "wrangler types",
    "check": "oxlint --fix --type-aware . && tsgo --noEmit",
    "deploy": "bun run cf-typegen && tsgo --noEmit && oxlint --fix --type-aware . && bun run format && wrangler deploy",
    "dev": "bun run cf-typegen && wrangler dev",
    "format": "prettier --write .",
    "lint": "oxlint --fix --type-aware .",
    "typecheck": "bun run cf-typegen && tsgo --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251125.0",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  },
  "dependencies": {
    "hono": "^4.10.6"
  }
}
