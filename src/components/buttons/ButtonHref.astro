---
import newWindowIcon from '/src/assets/icons/actions/new-window.svg'
import type { ImageMetadata } from 'astro';
import { Image } from 'astro:assets';
//
interface Props {
  buttonIcon: string;
  buttonTitle: string;
  buttonURL: string;
  buttonID: string;
}
const { buttonIcon, buttonTitle, buttonURL, buttonID } = Astro.props;
//
const brand_icon = import.meta.glob<{ default: ImageMetadata }>('/src/assets/icons/branding/*.{png,svg}')
if (!brand_icon[buttonIcon]) throw new Error(`"${buttonIcon}" does not exist in glob.`);
---

<button id={buttonID} 
  class="">
  <Image 
    src={brand_icon[buttonIcon]()}
    alt={buttonTitle}
    class="" 
  />
  <span>
    {buttonTitle}
  </span>
  <Image
    src={newWindowIcon}
    alt=""
    class=""
  />
</button>

<script>
// Use template literals to ensure buttonID and buttonURL are correctly interpreted as strings
document.getElementById('${buttonID}').addEventListener('click', function() {
  window.open('${buttonURL}', '_blank');
});
</script>